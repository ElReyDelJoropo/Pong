export const dimensions = {
  width: 800,
  height: 600,
};

export enum GameTextures {
  ball = 'ball',
  paddle = 'paddle',
}

export enum GameScenes {
  bootloader = 'bootloader',
  menu = 'main-menu',
  game = 'main',
  pause = 'pause',
}
